{
  "name": "infinity-shop",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm --prefix backend start",
    "build": "npm --prefix frontend ci && npm --prefix frontend run build"
  },
  "engines": {
    "node": "16.x || 18.x || 20.x"
  }
}
